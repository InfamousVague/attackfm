# AttackFM work session — 2026-08-13

This is the project-side index for the AttackFM work completed on 2026-08-13.
It records what changed, what was verified, what is live, and what remains.

## Server and import setup

- Repaired the SpotiFLAC Python environment and installed its missing
  dependencies.
- Connected AttackFM to the correct SpotiFLAC executable and enabled its plugin
  repository.
- Verified Spotify search and the Music Import plugin bundle.
- Configured the Spotify OAuth callback as
  `https://clio.tail3f61cd.ts.net:8443/api/spotify/callback`; this exact value
  must also be registered in the Spotify developer dashboard.
- Fixed local Ollama compatibility by removing the unsupported thinking flag
  while retaining strict structured responses.
- Installed and verified `nomic-embed-text`, including 768-dimensional
  embeddings and structured Qwen output.

## AI queue and Android work

- Added one-tap custom AI queue generation from a song action.
- Tightened song-seed analysis so audible traits drive similarity and
  soundtrack, franchise, biographical, and cultural context do not masquerade
  as sonic evidence.
- The seed mix ranking established in this session uses 50% semantic song-trait
  similarity, 38% measured musical features, and 12% recent listening history.
- Added the album-page AI DJ flow and fixed the album-page behavior covered by
  the latest Android test build.
- Fixed the Android timeline scrubber so taps and short drags reliably commit a
  seek.
- The latest session build was produced for the S22 Ultra as ARM64-only, its APK
  signature was verified, and the superseded APK was moved to Trash.
- Latest session handoff path:
  `/home/kevin/Desktop/Network/AttackFM-AI-DJ-Test/AttackFM-AI-DJ-0.3.30-S22-Ultra-latest.apk`

## Background enrichment audit

- Confirmed the server-side worker was active against a 667-track library.
- At audit time, 126 tracks had enrichment and all 126 had 768-dimensional
  vectors.
- The original enrichment was useful but uneven: 29 title-only summaries, 31
  summaries under 40 characters, 57 generic `This recording...` summaries,
  prompt-heading leakage, and unsupported production/instrumentation claims.
- The original quality was assessed at roughly 6/10: useful as a secondary
  recommendation signal, not trustworthy as primary sonic analysis.

## Enrichment v2

- Added conservative MusicBrainz title/artist matching, MBID persistence,
  recording tags, and a 1.1-second API courtesy delay.
- Replaced the old loose output with separate genres, sonic traits, moods,
  lyrical themes, confidence, and evidence provenance.
- Added measured BPM, energy, brightness, and loudness to the model evidence.
- Added normalization and validation that rejects title-only or generic
  summaries, category headings, duplicates, malformed terms, incomplete
  profiles, and low-confidence output.
- Added additive database fields for the new profile structure while preserving
  compatibility with the existing `ai_vibes` field.
- Legacy rows are reconsidered once by v2. Rejected attempts receive a
  `rejected_v2` stamp and no recommendation vector, preventing a bad row from
  blocking the durable queue.
- Live testing exposed structurally valid but weak profiles. Confidence was
  tightened so MusicBrainz identity without descriptive tags receives less
  credit, the acceptance floor became 0.45, and two weak early rows were
  quarantined with their vectors cleared.

Full design, schema, worker timing, validation behavior, and future roadmap:
[`AI_DJ_ENRICHMENT.md`](AI_DJ_ENRICHMENT.md).

## Verification and live state

- New enrichment unit tests: 2 passed, 0 failed.
- Full server `cargo check`: passed.
- Full debug build: passed.
- Additive database migration: observed live.
- MBID and structured v2 persistence: observed live.
- `attackfm-ai-dj-test.service` was restarted and verified active.
- Test server: `http://192.168.1.195:8788`.
- A `404` response at `/` is expected because no root route is defined.
- Background enrichment pauses during playback or active imports and resumes
  afterward.

## Remaining work and decisions

- [x] MusicBrainz identity/tags and evidence-aware enrichment v2.
- [x] Restrained liked-song tie-breaker: the custom queue now reserves 8% of
  its score for a positive like signal; unliked songs receive no penalty.
- [x] First deeper local-analysis pass: dynamic range and rhythmic activity are
  measured from decoded audio, persisted additively, supplied to enrichment,
  and used in collection-seed feature similarity.
- [x] Replace the blocked Last.fm plan with keyless ListenBrainz metadata,
  popularity context, and radio relationships. Results are cached durably,
  normalized, source-labelled, and identity-checked.
- [x] Decide narrowly targeted web search is unnecessary unless evaluation
  later demonstrates a structured-source gap.
- [x] Add a durable local audio embedding from five normalized signal features.
- [x] Split ranking into independently inspectable sonic, lyrical, community,
  measured-audio, and taste evidence families.
- [x] Keep lyrics, external knowledge, and measured sound as separate evidence
  families so lyrical or cultural similarity cannot dominate sonic matching.

## Continuation update — 2026-08-13 evening

- Reconfirmed the test service active on port 8788 and preserved all existing
  uncommitted server, frontend, and Android work.
- Confirmed no Last.fm API key is configured under the supported environment or
  user-service configuration. No credential value was read or exposed.
- Reconstructed the roadmap above and found the proposed liked-song adjustment
  already implemented at a deliberately restrained 8% positive-only weight.
- Added `dynamic_range` and `rhythmic_activity` to `track_features`. Existing
  rows with either value missing are automatically and durably backfilled by
  the polite one-track-at-a-time audio worker.
- Dynamic range uses the 90th-to-10th percentile short-window RMS gap; rhythmic
  activity uses adjacent short-window RMS change. Both are normalized to 0–1.
- Collection-seed ranking now compares these two measurements in addition to
  BPM, energy, and brightness. Enrichment prompts receive them as measured
  evidence rather than inferred prose.
- Validation: full server test suite passed with 17 tests passing and 2
  intentionally ignored network/build tests; full debug build and `cargo
  check` passed. The rebuilt user service restarted cleanly, remained active,
  and returned the expected HTTP 404 at `/`. Pre-existing unrelated compiler
  warnings remain.
- The additive migration ran at service startup. The backfill worker starts
  after its normal 30-second delay and remains subordinate to playback/import
  activity; no destructive bulk re-analysis was forced.

## ListenBrainz continuation

- Verified public ListenBrainz metadata, recording-popularity, and artist-radio
  endpoints live without an account or API key.
- Added exact-MBID and returned artist/title checks before accepting metadata.
- Added richer community tags and credited instrumentation/vocals to external
  evidence while keeping popularity out of sonic claims.
- Added durable related-recording MBIDs and listen/listener counts. Existing v2
  rows with an MBID but no ListenBrainz check are queued once for backfill.
- Added a 4% positive-only collaborative corroborator to custom queue ranking;
  semantic, specialized, measured, personal-history, and liked-song evidence
  retain 96% of the score.
- Final work is now sequenced as separate evidence-family vectors and match
  explanations, then genuine audio embeddings, then evaluation-set calibration
  and client-facing explanations. Targeted web retrieval is optional.

## Final AI DJ phase completed

- Added separate durable sonic, lyrical, and community vectors with automatic
  backfill while retaining the combined compatibility vector.
- Added a private deterministic local audio embedding from five decoded-signal
  measurements and integrated it into collection-seed ranking.
- Calibrated the outer score to 76% relevance, 20% account history/likes, and
  4% ListenBrainz corroboration. Recent exclusions and artist caps remain.
- Added structured score breakdowns and strongest-match reasons to queue API
  responses and surfaced the opening reason in the client.
- Added ranking tests for audio proximity, embedding bounds, and protection
  against personal/community signals dominating relevance.
- Validation reached 20 passing server tests with 2 intentionally ignored;
  server debug and production TypeScript/Vite builds passed.

## Original checklist closure

- Replaced the provisional five-value “embedding” as the preferred comparison
  with a versioned 48-dimensional spectral/temporal audio fingerprint computed
  directly from decoded recordings. The five measurements remain the fallback
  while the polite analyser backfills old rows.
- Added additive fingerprint schema, ranking integration, coverage reporting,
  and a unit test proving normalization and low/near/high spectral separation.
- Added a fixed five-seed, read-only evaluation set and executable benchmark for
  relevance proxy (album-mate retrieval), diversity, artist repetition,
  soundtrack/cultural false matches, and account-specific preference isolation.
- Initial evaluation across 697 tracks passed every fixed threshold. It used
  the fallback because fingerprint coverage began at 0/697 immediately after
  migration; the same benchmark will automatically use fingerprints as each
  seed is backfilled, making rollout comparison repeatable rather than anecdotal.
- Server tests after this work: 21 passed, 0 failed, 2 intentionally ignored.
  Debug build passed, the additive migration ran, and the test service restarted
  active. The original implementation checklist is now closed; fingerprint
  coverage is ongoing operational backfill, not missing implementation.

## Source-control note

The workspace contained pre-existing uncommitted frontend and Android work.
Those changes were preserved. This session documentation does not claim a git
commit or remote repository push.

## Final LAN verification and APK handoff

- Re-ran the fixed AI DJ evaluation against the live 704-track database. All
  thresholds passed, per-user separation passed, and fingerprint coverage had
  advanced to 398/704 tracks.
- Re-ran the server suite (21 passed, 0 failed, 2 intentionally ignored) and
  the production TypeScript/Vite build successfully.
- Confirmed `attackfm-ai-dj-test.service` active and listening on the LAN at
  `http://192.168.1.195:8788`; the root route returned the expected HTTP 404.
- Built a fresh ARM64-only Android debug APK, verified its APK v2 signature and
  `arm64-v8a` native library, and copied it to the mounted SMB share as
  `/home/kevin/Desktop/Network/AttackFM-AI-DJ-Test/AttackFM-AI-DJ-0.3.30-S22-Ultra-2026-08-13-final.apk`.
- Source and copied APK SHA-256 matched:
  `4d7f0736ee170c30997f0a654f8d15f8f18a04a727af98b2448830e24c36b29b`.
- The share is `//192.168.1.28/Network`; macOS can connect using
  `smb://192.168.1.28/Network` and then open `AttackFM-AI-DJ-Test`.
