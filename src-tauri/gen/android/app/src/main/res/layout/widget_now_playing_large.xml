<?xml version="1.0" encoding="utf-8"?>
<!-- THE LARGE FACE: the Now Playing screen itself, as much of it as a home
     screen will hold.

     Cover-forward and centred, because at this size the cover IS the widget -
     the same decision the player's own screen makes. Same ids as the two faces
     next door; see the compact one's header. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/widget_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@drawable/widget_bg"
  android:gravity="center_horizontal"
  android:orientation="vertical"
  android:padding="18dp">

  <!-- Weight is the HEIGHT in a vertical column; a zero width here is a zero
       width, full stop, and that is exactly how the cover came out invisible
       while the plate underneath it was still taking its wash from the same
       bitmap. -->
  <ImageView
    android:id="@+id/widget_art"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:adjustViewBounds="true"
    android:contentDescription="@null"
    android:scaleType="fitCenter"
    android:src="@drawable/widget_art_placeholder" />

  <TextView
    android:id="@+id/widget_title"
    android:text="AttackFM"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="14dp"
    android:ellipsize="end"
    android:gravity="center"
    android:maxLines="1"
    android:textColor="#FFFFFFFF"
    android:textSize="17sp"
    android:textStyle="bold" />

  <TextView
    android:id="@+id/widget_artist"
    android:text="Nothing playing"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:ellipsize="end"
    android:gravity="center"
    android:maxLines="1"
    android:textColor="#A6FFFFFF"
    android:textSize="13sp" />

  <TextView
    android:id="@+id/widget_line"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:ellipsize="end"
    android:gravity="center"
    android:maxLines="1"
    android:textColor="#8AFFFFFF"
    android:textSize="11sp"
    android:visibility="gone" />

  <ImageView
    android:id="@+id/widget_wave"
    android:layout_width="match_parent"
    android:layout_height="26dp"
    android:layout_marginTop="12dp"
    android:contentDescription="@null"
    android:scaleType="fitXY" />

  <LinearLayout
    android:id="@+id/widget_clocks"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="2dp"
    android:orientation="horizontal">

    <TextView
      android:id="@+id/widget_elapsed"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:textColor="#8AFFFFFF"
      android:textSize="11sp" />

    <TextView
      android:id="@+id/widget_remaining"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:gravity="end"
      android:textColor="#8AFFFFFF"
      android:textSize="11sp" />
  </LinearLayout>

  <LinearLayout
    android:id="@+id/widget_transport"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="10dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
      android:id="@+id/widget_heart"
      android:layout_width="38dp"
      android:layout_height="38dp"
      android:contentDescription="Favourite"
      android:padding="7dp"
      android:scaleType="fitCenter"
      android:src="@drawable/ic_widget_heart" />

    <ImageView
      android:id="@+id/widget_prev"
      android:layout_width="0dp"
      android:layout_height="46dp"
      android:layout_weight="1"
      android:contentDescription="Previous"
      android:padding="11dp"
      android:scaleType="fitCenter"
      android:src="@drawable/ic_widget_prev" />

    <ImageView
      android:id="@+id/widget_play"
      android:layout_width="58dp"
      android:layout_height="58dp"
      android:contentDescription="Play or pause"
      android:scaleType="fitCenter"
      android:src="@drawable/widget_play_preview" />

    <ImageView
      android:id="@+id/widget_next"
      android:layout_width="0dp"
      android:layout_height="46dp"
      android:layout_weight="1"
      android:contentDescription="Next"
      android:padding="11dp"
      android:scaleType="fitCenter"
      android:src="@drawable/ic_widget_next" />

    <ImageView
      android:layout_width="38dp"
      android:layout_height="38dp"
      android:contentDescription="@null"
      android:visibility="invisible" />
  </LinearLayout>
</LinearLayout>
