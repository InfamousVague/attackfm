<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.app" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- The system bars follow the SYSTEM DayNight theme, not the app's
             in-app choice - a dark app under a light system got a glaring
             white navigation bar. Transparent bars with contrast enforcement
             off let the app's own surface run to the screen's edge; the
             window background is the app's boot color so nothing flashes
             white behind them. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:windowBackground">#0f0f10</item>
    </style>
</resources>
