# Deploy target for `npm run redeploy` (copy to .env, which is gitignored).
# See scripts/redeploy.mjs. This is the same box PrettyCardboard deploys to.
AFM_DEPLOY_HOST=your.vps.ip
AFM_DEPLOY_USER=root
AFM_DEPLOY_PASS=your-ssh-password
