# Copy to src-tauri/gen/android/keystore.properties and fill in the passwords.
# That path is gitignored; this template is not. The .jks itself never moves
# into the repo.
storeFile=/Users/matt/.attackfm/android-release.jks
storePassword=
keyAlias=
keyPassword=
